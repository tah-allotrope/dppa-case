---
title: "DPPA Worksheets + Answer Summary — Combined Bilingual Word Doc"
date: "2026-06-29"
status: "complete — bulk-corrected 2026-07-31 per directive: plan predates 2026-07-20 and is presumed fully implemented (NOT individually verified against git/code evidence)"
request: "Combine the lessons/ worksheets with the DPPA scenario answer summary into a new Word doc following the style/format of lessons/DPPA_Scenario_Answer_Summary.docx."
plan_type: "multi-phase"
research_inputs:
  - "research/2026-06-29_dppa-worksheet-answer-docx-brainstorm.md"
  - "research/2026-06-29_dppa-scenario-numbers-spec.md"
---

# Plan: DPPA Worksheets + Answer Summary — Combined Bilingual Word Doc

## Objective
Produce a new bilingual (EN | VI) Word document, `lessons/DPPA_Worksheets_and_Answers.docx`, that — per scenario (S1 matched, S2 shortfall, S3 excess, then a negotiation grid) — pairs the blank compute worksheet with its worked totals, in the exact house style of `lessons/DPPA_Scenario_Answer_Summary.docx`. It gives a facilitator one printable "compute it, then check it" handout that visually matches the existing answer-summary deck.

## Context Snapshot
- **Current state:**
  - Blank worksheets in `lessons/0011-worksheets.html` (+ `-vi`, `-zh-cn`): per-scenario 5-line compute grids (the *Calculation* column pre-filled with the formula, the *VND/month* column blank) + C_EVN/CfD/C_KH/effective rows; S2 adds the retail line-4; S3 adds an excess block; plus a negotiation grid. The `-vi` file holds the official Vietnamese cell text.
  - Style/answer reference `lessons/DPPA_Scenario_Answer_Summary.docx` (A4, 0.75in margins, 56 paragraphs + 23 tables, EN | VI). Construction confirmed by inspection: title/scenario **banners are shaded paragraphs** (`w:shd fill=1F4E79`, white 18pt bold + `D6E4F0` 14pt VI subtitle + 9pt legal line, centered); section heads = bold 12pt `1F4E79`; sub-heads = bold `2E75B6`; **data tables** use a `1F4E79` header row with white text (e.g., T14/T17 `Item | Value/English | Giá trị/Tiếng Việt`; T22 4×5 `Scenario Comparison Summary`); `D6E4F0` light-blue 1-cell callout boxes; `FFFFFF` note tables; gray `888888` 8pt footer "Prepared for CEBA 2026 Training | Allotrope Partners Vietnam | Based on Decree 57/2025 & 243/2026".
  - Canonical totals in `research/2026-06-29_dppa-scenario-numbers-spec.md`: S1 C_EVN 8,563,196,000 / CfD +500,000,000 / C_KH 9,063,196,000 (~1,813); S2 19,628,262,400 / −800,000,000 / 18,828,262,400 (~2,092); S3 8,304,644,000 / +750,000,000 / 9,054,644,000 (~1,811). Constants: k×K_pp 1.034208, service 360, clearing 163.30 (fees 523.30), retail 2,204.
  - `python-docx 1.2.0` available; repo convention is python-docx/pptx generator scripts. No combined worksheet+answer doc exists.
- **Desired state:** `lessons/DPPA_Worksheets_and_Answers.docx` generated by a committed, re-runnable script: title banner → shared constants/legal-basis block → for each of S1/S2/S3: scenario banner → "Worksheet | Phiếu tính" blank grid → "Answer (totals) | Đáp án" (C_EVN/CfD/C_KH) → page break → negotiation grid + guidance → closing 3-case comparison table → footer. All totals verbatim from the spec; EN | VI; styling indistinguishable from the reference.
- **Key repo surfaces:**
  - `lessons/DPPA_Scenario_Answer_Summary.docx` — style template (copied, not edited) + footer/constants source.
  - `lessons/0011-worksheets.html` / `-vi.html` — grid row structure + EN/VI cell text.
  - `research/2026-06-29_dppa-scenario-numbers-spec.md` — totals + constants (verbatim).
  - `build_worksheet_answer_docx.py` (new) — the generator.
  - `lessons/DPPA_Worksheets_and_Answers.docx` (new) — the output.
- **Out of scope:** a zh-cn version; editing/replacing `DPPA_Scenario_Answer_Summary.docx`; the reference's IP/duck-curve/CFO narrative prose; full line-by-line worked answers; any Google Docs/browser action.

## Research Inputs
- `research/2026-06-29_dppa-worksheet-answer-docx-brainstorm.md` — fixes the deliverable, structure (per-scenario blank→totals), scope (S1/S2/S3 + negotiation), bilingual EN | VI, totals-only answers, comparison table, style-shell-only carry-over, output path (new file), copy-template build method, negotiation guidance, and DEC-001…010 + the two open questions (carried to Grill Me).
- `research/2026-06-29_dppa-scenario-numbers-spec.md` — the only source for every numeric total/constant the doc prints; copied verbatim.

## Assumptions and Constraints
- **ASM-001:** All totals/constants copied verbatim from the numbers spec; no re-derivation.
- **ASM-002:** Blank grids mirror `0011`: *Calculation* column shows the formula text; *VND/month* column is an empty, lightly-shaded fillable cell; rows = lines 1–4, C_EVN, line 5 CfD, C_KH, effective. S2 includes the retail line-4 row; S3 adds excess-block rows (excess volume, spot value, foregone CfD) as blanks.
- **ASM-003:** One scenario per page (page break between scenarios), matching the reference paging. A4, 0.75in margins.
- **ASM-004:** Bilingual cell/label text reuses `0011-worksheets-vi.html` + the reference docx so VI terminology is consistent (Giá thực hiện, Phụ tải, Sản lượng khớp).
- **CON-001:** No LibreOffice/Word on this machine for automated PDF/PNG render (per prior repo learnings), so visual QA is structural (python-docx dump) + a manual open-in-Word check; no rendered-image verification in-session.
- **CON-002:** python-docx has limited high-level shading support; banner/table fills require direct XML (`w:shd`, `w:tcPr`). Mitigated by copying the reference and cloning its existing `w:shd`/run patterns.
- **DEC-001:** Per scenario: banner → blank grid → worked totals; then comparison summary. (brief DEC-001/005)
- **DEC-002:** Scenarios = workshop S1/S2/S3 + negotiation, in the reference's style. (brief DEC-002)
- **DEC-003:** Bilingual EN | VI. (brief DEC-003)
- **DEC-004:** Answers = totals only (C_EVN/CfD/C_KH). (brief DEC-004)
- **DEC-005:** Style shell only (banner + constants/legal-basis + footer; no narrative). (brief DEC-006)
- **DEC-006:** New file `lessons/DPPA_Worksheets_and_Answers.docx`; original untouched. (brief DEC-007)
- **DEC-007:** Build = copy reference as template, extend (inherit exact styles), replacing its scenario bodies with S1/S2/S3 content. (brief DEC-008/010; Grill Me Q-001 confirms replace-vs-keep)
- **DEC-008:** Negotiation answer = brief guidance + worked example, not a single strike. (brief DEC-009)

## Phase Summary
| Phase | Goal | Dependencies | Primary outputs |
|---|---|---|---|
| PHASE-01 | Extract the template's exact style primitives + lock the per-scenario content/data spec | None | Style-constant table + content data (in the build script header) |
| PHASE-02 | Scaffold the generator: copy template, clear body, build style-helper functions, title banner + constants block + footer | PHASE-01 | `build_worksheet_answer_docx.py` (scaffold) producing a styled shell |
| PHASE-03 | Populate S1/S2/S3 (blank grid + totals) + negotiation + comparison table, bilingual | PHASE-02 | Complete `lessons/DPPA_Worksheets_and_Answers.docx` |
| PHASE-04 | Verify (structure + number cross-check + parity), record, commit | PHASE-03 | Verification log; `learning-records/0004`; commit |

## Detailed Phases

### PHASE-01 - Extract style primitives + lock content data
**Goal**
Codify the reference's exact formatting so the generator reproduces it, and assemble the per-scenario content (grid rows EN/VI, totals, comparison data, negotiation text) in one place.

**Tasks**
- [ ] TASK-01-01: Record the style constants (already inspected): banner shd `1F4E79`, white 18pt bold / `D6E4F0` 14pt / 9pt legal, centered; section head bold 12pt `1F4E79`; sub-head bold `2E75B6`; table header-row fill `1F4E79` white; callout fill `D6E4F0`; note fill `FFFFFF`; footer 8pt `888888`; A4 page, 0.75in margins. Capture EMU↔pt (228600=18pt, 177800=14pt, 152400=12pt, 114300=9pt, 101600=8pt).
- [ ] TASK-01-02: Transcribe the S1/S2/S3 grid rows (line label · EN/VI · *Calculation* formula · which cells are blank) from `0011-worksheets.html` + `-vi.html`. Note S2's retail line-4 and S3's excess block.
- [ ] TASK-01-03: Pull the totals per scenario from the numbers spec (C_EVN/CfD/C_KH/effective) for the answer blocks; pull the 3-case comparison rows.
- [ ] TASK-01-04: Draft the negotiation grid (blank proposal rows) + the guidance/worked-example text (CfD = (strike − 1,150) × 5,000,000; crosses zero at strike = FMP; gate trade-off), EN/VI.

**Files / Surfaces**
- `lessons/DPPA_Scenario_Answer_Summary.docx` — style source (read).
- `lessons/0011-worksheets.html`, `-vi.html` — grid text (read).
- `research/2026-06-29_dppa-scenario-numbers-spec.md` — totals/constants (read).

**Dependencies**
- None.

**Exit Criteria**
- [ ] A single content+style spec (constants, per-scenario grids EN/VI, totals, comparison rows, negotiation text) exists in the build script header/data dicts, ready to render.

**Phase Risks**
- **RISK-01-01:** VI text drift between `0011-vi` and the reference. Mitigation: prefer the reference's VI terminology where they differ; keep numbers identical.

### PHASE-02 - Scaffold the generator (template copy + style helpers)
**Goal**
A runnable `build_worksheet_answer_docx.py` that copies the template, strips its scenario body, and emits a correctly-styled shell (title banner, constants/legal-basis block, footer) — no scenario content yet.

**Tasks**
- [ ] TASK-02-01: `shutil.copy('lessons/DPPA_Scenario_Answer_Summary.docx', 'lessons/DPPA_Worksheets_and_Answers.docx')`; open the copy with python-docx.
- [ ] TASK-02-02: Remove all body paragraphs and tables (iterate `document.element.body`, drop `w:p`/`w:tbl` except the trailing `w:sectPr`) so only an empty styled body + section settings remain. Keep margins/page size (A4).
- [ ] TASK-02-03: Write style-helper functions cloning the reference's XML: `banner(doc, en, vi, legal=None)` (centered paragraph(s) with `w:shd fill=1F4E79`, white 18pt bold / `D6E4F0` 14pt / 9pt); `section_head(doc, text)` (bold 12pt `1F4E79`); `subhead(doc, text)` (bold `2E75B6`); `data_table(doc, rows, header_fill='1F4E79')` (header row white-on-blue, EN|VI columns); `callout(doc, text, fill='D6E4F0')`; `fillable_table(...)` (worksheet grid with empty lightly-shaded answer cells); `footer_line(doc, text)` (8pt `888888`). Set fills via direct `w:shd` XML (helper `set_cell_shading`/`set_para_shading`).
- [ ] TASK-02-04: Emit the title banner ("DPPA Worksheets & Answers | Phiếu tính & Đáp án" + legal-basis line) and the shared **constants/legal-basis block** (k×K_pp 1.034208, service 360, clearing 163.30, fees 523.30, retail 2,204; Decree 57/243) as a `data_table`, and the gray footer. Save and confirm it opens.

**Files / Surfaces**
- `build_worksheet_answer_docx.py` (new) — scaffold + helpers.
- `lessons/DPPA_Worksheets_and_Answers.docx` (new) — styled shell output.

**Dependencies**
- PHASE-01.

**Exit Criteria**
- [ ] Script runs clean; output opens as valid .docx with the title banner, constants block, and footer in the reference's styling (verified via the dump script: shd fills + sizes match).

**Phase Risks**
- **RISK-02-01:** Body-clear also removes the `w:sectPr` (page/margins). Mitigation: detach and re-append `sectPr`, or skip it when removing; verify A4 + margins survive.
- **RISK-02-02:** Cloned `w:shd` not applied at the right XML level (paragraph vs run vs cell). Mitigation: mirror the exact element path observed (banner = paragraph `pPr/w:shd`; table = `tcPr/w:shd`).

### PHASE-03 - Populate scenarios, negotiation, and comparison
**Goal**
Fill the shell with the bilingual S1/S2/S3 worksheet+totals blocks, the negotiation block, and the comparison summary.

**Tasks**
- [ ] TASK-03-01: For each scenario S1/S2/S3, emit: a **scenario banner** (e.g., "SCENARIO 1 — MATCHED | KỊCH BẢN 1 — KHỚP", with the inputs line: Q_c/Q_KH/FMP/strike); a `subhead` "Worksheet | Phiếu tính"; a **fillable grid** (lines 1–4 with formula text in *Calculation*, blank shaded answer cells; C_EVN/line-5 CfD/C_KH/effective rows blank); a `subhead` "Answer (totals) | Đáp án"; a small **totals table** filled with C_EVN/CfD/C_KH (+ effective) from the spec; then a page break. Include S2's retail line-4 and S3's excess-block rows.
- [ ] TASK-03-02: Emit the **negotiation** block: a banner, a blank proposal grid (proposed strike / CfD / flows-to), then a `callout` with the guidance + worked example (CfD = (strike − 1,150) × 5,000,000; crosses zero at strike = FMP; buyer/seller/lender gate trade-off), EN | VI.
- [ ] TASK-03-03: Emit the closing **3-case comparison table** (modeled on the reference's T22): columns Scenario / volume axis / line 4 / FMP vs strike / CfD / C_EVN / C_KH / effective / risk lesson, rows S1/S2/S3, EN | VI headers; header row `1F4E79`.
- [ ] TASK-03-04: Re-run the generator end to end; confirm the doc is complete.

**Files / Surfaces**
- `build_worksheet_answer_docx.py` — content population.
- `lessons/DPPA_Worksheets_and_Answers.docx` — full output.

**Dependencies**
- PHASE-02.

**Exit Criteria**
- [ ] Output contains S1/S2/S3 (each: banner + blank grid + totals) + negotiation + comparison, all bilingual, one scenario per page; blank answer cells are empty and shaded; totals match the spec.

**Phase Risks**
- **RISK-03-01:** Blank fillable cells render too small to write in. Mitigation: set a minimum row height / cell width on the answer column.
- **RISK-03-02:** Long EN|VI strings overflow A4 width. Mitigation: use two-column table layout (EN | VI) sized to margins, as the reference does.

### PHASE-04 - Verify, record, commit
**Goal**
Prove correctness and ship.

**Tasks**
- [ ] TASK-04-01: Run the dump script on the output; assert: title banner shd `1F4E79`; constants block present; each scenario banner + grid + totals present; totals strings present byte-for-byte (`8,563,196,000`, `+500,000,000`, `9,063,196,000`, `19,628,262,400`, `-800,000,000` / `−800,000,000`, `18,828,262,400`, `8,304,644,000`, `+750,000,000`, `9,054,644,000`); comparison table present; footer present.
- [ ] TASK-04-02: VI parity — confirm each scenario block has both EN and VI text (spot-check the Giá thực hiện / Phụ tải / Sản lượng khớp terms); confirm no zh-cn leaked in.
- [ ] TASK-04-03: Number cross-check vs `research/2026-06-29_dppa-scenario-numbers-spec.md`; 0 mismatches.
- [ ] TASK-04-04: Note the visual-QA limitation (no LibreOffice render); leave the file for a manual open-in-Word pass by the user.
- [ ] TASK-04-05: Record `learning-records/0004-worksheet-answer-docx.md`; add a NOTES.md line; update `activeContext.md`.
- [ ] TASK-04-06: Stage only the new files (`build_worksheet_answer_docx.py`, `lessons/DPPA_Worksheets_and_Answers.docx`, records) + this plan/brief; commit + push to `master`. Do not commit the pre-existing `.firebase` cache or untracked `To Teach.pptx`.

**Files / Surfaces**
- `lessons/DPPA_Worksheets_and_Answers.docx`, `build_worksheet_answer_docx.py`, `learning-records/0004-*.md`, `NOTES.md`, `activeContext.md`.

**Dependencies**
- PHASE-03.

**Exit Criteria**
- [ ] Dump assertions pass; totals match spec; EN+VI present; committed + pushed; visual-QA handoff noted.

**Phase Risks**
- **RISK-04-01:** A number typo slips through. Mitigation: TASK-04-01/03 grep against the spec is the gate; totals are also re-quotable from `app/src/modules/settlement.js` if needed.

## Verification Strategy
- **TEST-001 (structure dump):** Run the dump script on `lessons/DPPA_Worksheets_and_Answers.docx`; assert banner shd fills, scenario count, table count, footer.
- **TEST-002 (number anchor):** Grep the output's text for each spec total; pass = all present, 0 foreign numbers in the totals tables.
- **MANUAL-001 (open in Word):** User opens the .docx; confirm banners/colors/tables match the reference and the blank cells are writable. (No in-session render — CON-001.)
- **MANUAL-002 (locale):** Each scenario block shows EN and VI; no zh-cn.
- **DATA-001 (constants):** The constants/legal-basis block shows k×K_pp 1.034208, 360, 163.30, 523.30, 2,204, Decree 57/243.

## Risks and Alternatives
- **RISK-001:** Style fidelity drift (fills/sizes not matching the reference). Mitigation: copy-template approach + clone observed `w:shd`/run XML; TEST-001 compares fills.
- **RISK-002:** No automated visual render (CON-001) means a layout bug could ship unseen. Mitigation: structural dump + explicit manual open-in-Word handoff before the doc is used live.
- **ALT-001 (declined):** Build fresh with python-docx (no template). Rejected (DEC-007) — higher visual-drift risk.
- **ALT-002 (declined):** Two-part blanks-then-key layout. Rejected (DEC-001) — per-scenario blank→answer chosen.
- **ALT-003 (declined):** zh-cn version. Out of scope (matches the EN|VI reference).

## Grill Me
1. **Q-001:** Confirm "copy & extend" = the new doc **replaces** the reference's two scenarios (IP, Duck Curve) with the workshop S1/S2/S3 + negotiation, keeping only its shell/styles?
   - **Recommended default:** Yes — template supplies styling/shell only; content = S1/S2/S3 + negotiation (DEC-002/005/007).
   - **Why this matters:** If the original two scenarios must be retained and S3/worksheets appended, the doc roughly doubles and mixes two numbering schemes.
   - **If answered differently:** PHASE-02 keeps the reference body and PHASE-03 appends, instead of clearing and rebuilding.
2. **Q-002:** Per-scenario worked totals **immediately under each blank grid** (visible while computing), or hidden as a back-of-doc answer key?
   - **Recommended default:** Inline under each grid, with a thin rule + "Answer / Đáp án" label (DEC-001 reads as inline).
   - **Why this matters:** Decides whether the same printout doubles as a blanks-only handout.
   - **If answered differently:** Move all totals to a final "Answer Key | Đáp án" section (contradicts DEC-001; PHASE-03 re-sequences).

## Suggested Next Step
Answer Q-001/Q-002 (defaults are safe), then execute PHASE-01 → PHASE-04. Both Grill Me defaults are already encoded in the phases, so implementation can begin immediately if you accept them.
